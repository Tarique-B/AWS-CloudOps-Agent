# Run agent locally with docker compose
services:
  # FastAPI Agent Service
  agent:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: aws-assistant-agent
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - LOG_LEVEL=INFO
      - BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
      # AWS credentials - pass through from host
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION:-us-east-1}
      # - AWS_SESSION_TOKEN=${AWS_SESSION_TOKEN}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - aws-assistant-network

  # Streamlit Chatbot Service
  chatbot:
    build:
      context: .
      dockerfile: Dockerfile.app
    container_name: aws-cloudops-assistant
    ports:
      - "80:80"
    environment:
      - API_BASE_URL=http://agent:8080
      - LOG_LEVEL=INFO
    depends_on:
      agent:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - aws-assistant-network

networks:
  aws-assistant-network:
    driver: bridge

